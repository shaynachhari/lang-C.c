#include<stdio.h>

int main(){
    // to take input for a character check and print wether the chacter is an alphabet or not?
    /*   char ch;
    printf("Enter any character  ");
    scanf("%c",&ch);
 
    if ((ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z'))
    printf("it is an alphabet   ");
    else
    printf("it is not an alphabet   ");  */



    // print your alphabet only upper case
  /*  char ch;
    printf("Enter any upper character  ");
    scanf("%c",&ch);

    if (ch >= 'A' && ch <= 'Z')        // if (ch  >= 65  && ch <= 90)

    printf("it is a upper character  ");
    else
    printf("it is a not upper character   ");  */



    // print your alphabet only lower case


    /*  char ch;
    printf("\nEnter any lower character  ");
    scanf("%c",&ch);

    if (ch >= 'a' && ch <= 'z')        // if (ch  >= 65  && ch <= 90)

    printf("it is a lower character  ");
    else
    printf("it is a not lower character   ");  */



    // war to take input for a character check and print whether it is a digit or not?

    char ch;
    printf("Enter any chacchter  ");
    scanf("%c",&ch);

    if (ch >= '0' && ch <= '9')

    printf("it is a digit  ");

    else
    printf("it is not a digit  ");


    // war to take input for a character check and print wether it is a (vowel) or not ?






    return 0;
}